#include<iostream>
#include<stdlib.h>

struct Node{
	int data;
	struct Node * llink;
	struct Node * rlink;

};

Node * creator(){
	Node * ptr = Node * malloc(sizeof(Node));
	return ptr;

}

int main(){
	struct Node * node;
	node->data;
	Node * ptr =creator()
}